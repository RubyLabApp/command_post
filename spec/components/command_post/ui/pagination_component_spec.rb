require "rails_helper"

# PaginationComponent requires Pagy::Frontend which is only loaded in full Rails context
# This component is tested through integration/system tests
# Coverage for this component is achieved through the engine's integration test suite
