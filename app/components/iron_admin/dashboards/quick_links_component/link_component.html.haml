= link_to href, class: "flex items-center gap-4 px-6 py-4 #{theme.table_row_hover} transition-colors group" do
  - if icon
    .flex-shrink-0.p-2.rounded-lg.bg-indigo-50.group-hover:bg-indigo-100.transition-colors
      = heroicon icon, variant: :outline, options: { class: "h-5 w-5 text-indigo-600" }
  .flex-1.min-w-0
    %p.text-sm.font-medium{ class: theme.body_text }
      = label
    - if description
      %p.text-sm{ class: theme.muted_text }
        = description
  = heroicon "chevron-right", variant: :mini, options: { class: "h-5 w-5 #{theme.muted_text} group-hover:text-gray-700" }
